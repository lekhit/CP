{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Java: Build and Run",
            "type": "shell",
            "command": "cd ${fileDirname} &&  javac -cp ${fileDirname} ${fileBasenameNoExtension} ",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "always",
                "panel": "new"
            },
            "problemMatcher": []
        },
        
        {
            "type": "cppbuild",
            "label": "C/C++: clang++ build active file",
            "command": "g++",
            "args": [
                "-std=c++17",
                "-stdlib=libc++",
                "-O2",
                "-DLOCAL",
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": ["$gcc"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Create CP Template",
            "type": "shell",
            "command": "${workspaceFolder}/template_generator.sh",
            "args": [
                "${input:problemName}"
            ],
            "presentation": {
                "reveal": "always",
                "panel": "new"
            },
            "problemMatcher": []
        }
    ],
    "inputs": [
        {
            "id": "problemName",
            "description": "Enter problem folder name:",
            "default": "problem1",
            "type": "promptString"
        }
    ]
}